prepare:
  raw_data_path: "data/raw/green_tripdata_2024-01.parquet"
  prepared_data_path: "data/prepared/data.parquet"

train:
  target: "duration"
  n_estimators: 100
  max_depth: 6
  learning_rate: 0.1
  random_state: 42

evaluate:
  metrics_file: "reports/metrics.json"
